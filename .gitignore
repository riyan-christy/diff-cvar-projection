# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# experiment outputs (regenerated by run_all.sh; keep the dir, not the contents)
results/*
!results/.gitkeep

# the on-machine lock is generated by setup_lambda.sh; commit it deliberately
# (uncomment the next line if you do NOT want to track it)
# requirements.lock.txt

# os
.DS_Store
